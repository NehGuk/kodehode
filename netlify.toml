[build]
  base = "html-css-oppgave-3"
  command = "pnpm install && pnpm build"  # ← Changed from npm to pnpm
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--version"  # This forces Netlify to detect pnpm

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200